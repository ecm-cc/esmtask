module.exports = async (attachmentList, config, options) => {
    // TODO When API-Key and test attachments are available
    // Load attachments via ivanti
    // Upload attachments as esm documents
    console.log('Attachments arrived', JSON.stringify(attachmentList));
    console.log('config', config);
    console.log('options', options);
};
