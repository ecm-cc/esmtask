<label class="mdc-text-field mdc-text-field--outlined center-align property-field">
  <span class="mdc-notched-outline">
    <span class="mdc-notched-outline__leading"></span>
    <span class="mdc-notched-outline__notch">
      <span class="mdc-floating-label" id="{{id}}-label">{{title}}</span>
    </span>
    <span class="mdc-notched-outline__trailing"></span>
  </span>
  {{#if value}}
    <a class="mailto_input" href="mailto:{{value}}?subject={{revertSubject subject}}">
  {{/if}}
  <input type="text" id="{{id}}" class="mdc-text-field__input {{#if disabled}}input-disabled{{/if}}"
    aria-labelledby="{{id}}-label" value="{{#if value}}{{value}}{{/if}}">
  {{#if value}}
    </a>
  {{/if}}
</label>


{{#if helper}}
  <div class="mdc-text-field-helper-line">
    <div id="username-helper-text" class="mdc-text-field-helper-text" aria-hidden="true">
      {{helper}}
    </div>
  </div>
{{/if}}